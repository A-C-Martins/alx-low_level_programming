#include <stdio.h>
#include <string.h>

/**
 * main - Writes digit
 *
 * Return: 0 success
 */

int main(void)
{
	int digit = 10;

	for (int i = 0; i < digit; i++)
	{
		printf("%d", i);
	}
	putchar('\n');

	return (0);
}
