#include <stdio.h>
#include <string.h>
#includee "main.h"
/**
 * rev_string - reverses string
 * @s: string to be reversed
 *
 * Author - AC Martins:
 */
void rev_string(char *s)
{
	int i;
	char *tmp;

	for (i = 0; s[c] != 0; i++)
	{
	}
	for (i = i - 1; i >= 0; i--)
	{
		tmp = s[i];
	}
}
