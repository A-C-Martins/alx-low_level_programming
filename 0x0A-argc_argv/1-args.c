#include <stdio.h>

/**
 * main - program start up
 * @arcount: - Argument count
 * @argv: Agument vector
 *
 * Return: 0 success
 */

int main(int arcount, char *argv[])
{
	int i, j;

	for (i = 0; i < arcount; i++)
	{
		j = i;
	}
	printf("%d\n", j);

	(void)argv;

	return (0);
}
